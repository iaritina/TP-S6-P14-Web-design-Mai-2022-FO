import React from 'react';
import ShowContent from '../components/ShowContent';

const Content = () => {
    return (
        <div>
            <ShowContent />
        </div>
    );
};

export default Content;